application:
  queues:
    deleted-files: files-deleted-social-queue

spring:
  datasource:
    url: jdbc:postgresql://fileshare_social:5462/fileshares_social
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update